module.exports = (Robinhood, trend) =>{
    // find stocks to watch
    const stocksToWatch =;
    // watch them, (register watch listeners)

    // record picks when condition is met

    // profit
}